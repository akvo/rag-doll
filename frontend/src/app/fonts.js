import { Roboto_Condensed, Assistant } from "next/font/google";

export const roboto_condensed = Roboto_Condensed({
  subsets: ["latin"],
  display: "swap",
});

export const assistant = Assistant({ subsets: ["latin"], display: "swap" });
